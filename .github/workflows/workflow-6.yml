name: workflow-6

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install action-6 dependencies
        run: |
          cd action-6
          npm install

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install root dependencies (if present)
        run: |
          if [ -f package.json ]; then npm install; fi

      - name: Run custom action (artifact creator)
        uses: ./action-6
        with:
          example-input: 'This is a test artifact from custom action.'

      - name: Archive artifact.txt
        uses: actions/upload-artifact@v4
        with:
          name: action-6-artifact
          path: artifact.txt

      - name: List files in workspace
        run: ls -l
